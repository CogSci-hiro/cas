# ============================================================
# EVENTS (Canonical Event Table)
# ============================================================

events:
  ipu_min_duration_s: 0.30
  ipu_min_syllables: 3

  time_within_run:
    normalize: true
    method: "minmax_0_1"   # implemented as t_onset / T_run

  matching:
    offset_margin_s: 1.0
