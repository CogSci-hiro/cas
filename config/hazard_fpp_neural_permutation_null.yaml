input:
  riskset_path: "/Users/hiro/Datasets/working/cas/reports/hazard_neural_fpp/riskset/neural_fpp_hazard_table.parquet"

output:
  output_dir: "/Users/hiro/Datasets/working/cas/reports/hazard_neural_fpp/permutation_null"

permutation:
  neural_family: "all"
  n_permutations: 100
  seed: 12345
  n_jobs: 1
  event_column: "event_fpp"
  episode_column: "episode_id"
  participant_column: "participant_speaker_id"
  run_column: "run"
  delta_criterion: "bic"
  verbose: true
  max_permutations_for_smoke_test: null
  max_fit_rows: null
