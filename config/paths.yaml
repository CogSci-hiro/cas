# Shared project paths used across the workflow.
# Keep legacy flat keys for backward compatibility while gradually moving
# modules onto `output_dir` plus the structured `paths:` namespace.

project_root: "."
bids_root: "/Users/hiro/Datasets/hyperscanning-bids"
annotations_dir: "/Users/hiro/Datasets/hyperscanning-bids/derivatives/anais"
ica_dir: "/Users/hiro/Datasets/working/hyperscanning/precomputed_ica"
lm_feature_dir: "/Users/hiro/Datasets/hyperscanning-bids/derivatives/converted_lm_features/features/events"

output_dir: "/Users/hiro/Datasets/working/cas-refactoring"

paths:
  logs: "{output_dir}/logs"
  cache: "{output_dir}/cache"
  manifests: "{output_dir}/manifests"
  preprocessing: "{output_dir}/preprocessing"
  events: "{output_dir}/events"
  features: "{output_dir}/features"
  epochs: "{output_dir}/epochs"
  behavior: "{output_dir}/behavior"
  induced: "{output_dir}/induced"
  amplitude: "{output_dir}/amplitude"
  trf: "{output_dir}/trf"
  figures: "{output_dir}/figures"
  figures_main: "{output_dir}/figures/main"
  figures_supp: "{output_dir}/figures/supp"
  figures_qc: "{output_dir}/figures/qc"
  archive: "{output_dir}/archive"

# Legacy compatibility keys still consumed by non-preprocessing code.
derivatives_root: "/Users/hiro/Datasets/working/cas"
features_root: "/Users/hiro/Datasets/working/cas/features"
eeg_array_root: "/Users/hiro/Datasets/working/cas/eeg"
preprocessed_eeg_root: "/Users/hiro/Datasets/working/cas/preprocessing/eeg"
trf_root: "/Users/hiro/Datasets/working/cas/trf"
reports_root: "/Users/hiro/Datasets/working/cas/figures"
